function solve(array, str) {
    console.log(array.join(str));
}

solve(['One', 
'Two', 
'Three', 
'Four', 
'Five'], 
'-');
solve(['How about no?', 
'I',
'will', 
'not', 
'do', 
'it!'], 
'_');